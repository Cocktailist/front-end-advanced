import styled from "styled-components";
import { FlexRow, FlexRowBtwn } from "../../../commonStyles";

export const Wrapper = styled.div`
  ${FlexRowBtwn}
  padding: 0;
`;

export const SubWrapper = styled.div`
  ${FlexRow}
  padding: 0;
`;
